import React from 'react';

interface AppProps {
  name: string;
}

type ContainerProps = {
  padding?: string | 0;
  margin?: string | 0;
};



export default function App({ name }: AppProps) {
  return <div>hell</div>
}
